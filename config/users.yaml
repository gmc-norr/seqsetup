# SeqSetup local fallback credentials.
# Keep this file out of production builds and populate credentials per environment.
#
# Format:
# users:
#   username:
#     display_name: "User Name"
#     email: "user@example.com"
#     password_hash: "$2b$12$..."  # bcrypt hash
#     role: admin|standard

users: {}
